# DS_find_median
Create data structure that preserve the median value
Min heap and Max heap data structure is used
It has function to insert element into DS, find median of the DS and delete median from DS
